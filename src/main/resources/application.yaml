spring:
  application:
    name: architecture-comparison
  datasource:
    url: jdbc:postgresql://localhost:5432/architecture_comparison
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
